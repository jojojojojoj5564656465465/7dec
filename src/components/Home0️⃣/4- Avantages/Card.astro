---
import * as s from './index.css'
interface Props {
  icon: string
  qualité: string
  text: string
}
const { icon, qualité, text } = Astro.props
---

<div class={s.card.wrapper}>
  <i class={s.card.icon}>{icon}</i>
  <dt class={s.card.titre}>{qualité}</dt>
  <dd class={s.card.text}>
    {text}
  </dd>
</div>
